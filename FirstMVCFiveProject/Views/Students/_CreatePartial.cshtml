@model FirstMVCFiveProject.Models.Student

@using (Html.BeginForm("CreateAjax", "Students", FormMethod.Post, new { id = "create-form" }))
{
    @Html.AntiForgeryToken()
    <div class="mb-3">
        @Html.LabelFor(m => m.RollNo, new { @class = "form-label" })
        @Html.TextBoxFor(m => m.RollNo, new { @class = "form-control rounded" })
        @Html.ValidationMessageFor(m => m.RollNo, "", new { @class = "text-danger" })
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.Age, new { @class = "form-label" })
        @Html.TextBoxFor(m => m.Age, new { @class = "form-control rounded" })
        @Html.ValidationMessageFor(m => m.Age, "", new { @class = "text-danger" })
    </div>
    <div class="mb-3">
        @Html.LabelFor(m => m.Email, new { @class = "form-label" })
        @Html.TextBoxFor(m => m.Email, new { @class = "form-control rounded" })
        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
    </div>
    <button type="submit" class="btn btn-primary rounded">Create</button>
}
